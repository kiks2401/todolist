{% extends 'base.html.twig' %}

{% block body %}

  <h2 class="page-header">Edit - {{ todo.name }}</h2>

  {{form_start(form)}}
  {{form_widget(form)}}
  <button type="submit">Edit</button>
  {{form_end(form)}}

{% endblock body %}